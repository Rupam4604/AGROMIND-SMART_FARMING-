# Smart Farming V2.0

An ESP32-based IoT smart farming system with Firebase cloud integration.

## Features

-   ESP32 DevKit V1
-   DHT22
-   Soil moisture monitoring
-   Water level monitoring
-   Automatic Pump, Fan, Humidifier and Grow Light
-   20x4 LCD
-   Firebase Realtime Database
-   Auto/Manual modes
-   Scheduler-based firmware

## Hardware

-   ESP32 DevKit V1
-   DHT22
-   2 Soil Moisture Sensors
-   LDR
-   2 Water Level Sensors
-   4-Channel Relay
-   LCD 20x4 I2C
-   Pump, Fan, Humidifier, Grow Light
  ## Author

## SOFTWARE

# 🌱 AgroMind — Smart Farming AI Platform

A browser-based smart farming dashboard with **real AI plant disease detection** powered by your own MobileNetV2 model (`AgroMind.keras`), live weather, sensor simulation, and device control.

---

## 🚀 Quick Start (3 steps)

### Step 1 — Convert the model (run once)
```bash
pip install tensorflowjs tensorflow
python convert_model.py
```
This reads `public/model/AgroMind.keras` and outputs `public/model/tfjs_model/`.

### Step 2 — Start the app
```bash
npm install
npm start
```
**OR** in VS Code: right-click `public/index.html` → **Open with Live Server**

### Step 3 — Use it
1. Click **Sign In**
2. Go to **Disease Detection** in the sidebar
3. Drop a leaf photo → click **Analyse Disease**
4. Get instant AI results — runs 100% in your browser 🎉

---

## 📁 Project Structure

```
AgroMind/
├── public/
│   ├── index.html              ← Main app
│   ├── model/
│   │   ├── AgroMind.keras      ← ✅ Your trained model (bundled)
│   │   ├── tfjs_model/         ← ⚡ Generated by convert_model.py
│   │   │   ├── model.json
│   │   │   └── *.bin
│   │   └── README.txt
│   ├── css/
│   │   ├── tokens.css          ← 🎨 Colors & theme — edit here
│   │   ├── layout.css          ← App shell, header, sidebar
│   │   ├── components.css      ← Cards, buttons, badges
│   │   └── pages.css           ← Page-specific styles
│   └── js/
│       ├── config.js           ← ⚙️ API keys, crop settings
│       ├── data.js             ← Disease DB & class labels
│       ├── app.js              ← Boot, login, navigation
│       ├── weather.js          ← OpenWeatherMap API
│       ├── sensors.js          ← Sensor simulation
│       ├── charts.js           ← Chart.js line charts
│       ├── controls.js         ← Device & crop controls
│       ├── model.js            ← TF.js auto-load & inference
│       └── ui.js               ← Results, history, notifications
├── convert_model.py            ← 🔄 Model converter script
├── package.json
└── README.md
```

---

## 🧠 How the AI Works

| What | Detail |
|---|---|
| Architecture | MobileNetV2 (1.0, 160×160) |
| Classes | 39 PlantVillage crop disease classes |
| Input | 160×160 RGB image, normalised ÷127.5 − 1 |
| Output | 39 sigmoid probabilities |
| Runtime | TensorFlow.js 4.11.0, 100% in-browser |
| Model file | `AgroMind.keras` (Keras 3.13.2) |

The model auto-loads from `model/tfjs_model/model.json` when the page opens.
If the converted model isn't found, the app runs in **demo mode** automatically.

---

## 🌿 Supported Crops & Diseases (39 classes)

Apple · Blueberry · Cherry · Corn · Grape · Orange · Peach · Pepper · Potato · Raspberry · Soybean · Squash · Strawberry · Tomato

---

## ✏️ Customisation

| What to change | File |
|---|---|
| Theme colors / dark mode | `public/css/tokens.css` |
| Add a new crop | `public/js/config.js` → `cropConfig` |
| Edit disease descriptions | `public/js/data.js` → `DIS_DB` |
| AI class labels | `public/js/data.js` → `CLASSES` |
| Weather API key | `public/js/config.js` → `WEATHER_KEY` |

---

## 📦 npm Scripts

| Command | What it does |
|---|---|
| `npm start` | Start live-server on port 3000 |
| `npm run dev` | Same with file watching |

---

*Built with 🌱 — AgroMind Smart Farming Platform*


**Rupam Ghosh**
